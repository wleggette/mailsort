---
description: Mailsort project-specific rules
trigger: always_on
---

## Project Constraints

- Use existing codebase patterns (e.g., `Database` context manager, `MoveDecision` model, `AuditWriter` for run lifecycle).

_(This is the only mailsort-specific rule. All other development rules are in `general.md`.)_
